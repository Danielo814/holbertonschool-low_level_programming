#include "holberton.h"
/**
 * add - adds two integers
 * @i: first number
 * @j: second number to add to first number
 * Description: uses addition operator to add parameters
 * Return: results from addition of the two
 */
int add(int i, int j)
{
	int sum;

	sum = i + j;
	return (sum);
}

#include <stdio.h>
/**
 *
 *
 *
 *
 */
int main(void)
{
	int a, b, c, d;

	for (a = 0; a < 1024; a++)
	{
		b = a % 3;
		c = a % 5;
		if (b == 0 || c == 0)
		{
			d = d + a;
		}
	}
	printf("%d\n", d);
	return (0);
}

#include <stdio.h>
/**
 *
 *
 *
 *
 */
int main(void)
{
	int a;

	a = 'a';
	while (a < 'z')
	{
		putchar(a);
		a = a + 1;
	}
	a = 'A';
	while (a < 'Z')
	{
		putchar(a);
		a = a + 1;
	}
	putchar('\n');
	return (0);
}

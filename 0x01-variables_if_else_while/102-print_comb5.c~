#include <stdio.h>
/**
 *
 *
 *
 *
 */
int main(void)
{
	int a, b;
	int c, d, e, f;

	for (a = 0; a < 99; a++)
	{
		c = a / 10;
		d = a % 10;
		for (b = 0; b <= 99; b++)
		{
			e = b / 10;
			f = b % 10;
			if (a != b && a < b)
			{
				putchar(c + '0');
				putchar(d + '0');
				putchar(' ');
				putchar(e + '0');
				putchar(f + '0');
				if (a != 98 || b != 99)
				{
					putchar(',');
					putchar(' ');
				}
			}
		}
	}
	putchar('\n');
	return (0);
}
